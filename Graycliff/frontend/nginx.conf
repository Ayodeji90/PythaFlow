server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — deep links like /dashboard resolve to the app shell
    location / {
        try_files $uri $uri/ /index.html;
    }
}
