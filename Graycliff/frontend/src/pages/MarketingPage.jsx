export default function MarketingPage() {
  return (
    <div className="page" style={{ maxWidth: 640 }}>
      <h1 className="page-title">AI Marketing Studio</h1>
      <p className="page-sub">Coming online in this demo build — on-brand posts and newsletters, drafted by AI.</p>
      <div className="card">
        <p className="page-sub" style={{ margin: 0 }}>
          This module is being wired up (sales data + events → AI copywriter → approval queue).
          Check back after the next build step.
        </p>
      </div>
    </div>
  );
}
